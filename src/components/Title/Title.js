import React from 'react'
import Wrapper from './style'

const Title = (props) => (
    <Wrapper>
        <header>
            <h1>K1 Todo List</h1>
        </header>
    </Wrapper>
)
export default Title